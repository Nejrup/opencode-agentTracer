{
  "name": "opencode-agenttracer",
  "private": true,
  "type": "module",
  "scripts": {
    "clean:test-dist": "node -e \"require('node:fs').rmSync('tests/dist', { recursive: true, force: true })\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build:test": "npm run clean:test-dist && tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test tests/dist/tests/agentTracer.test.js && npm run clean:test-dist",
    "dryrun": "npm run build:test && node tests/dist/tests/agentTracer.dryrun.js && npm run clean:test-dist"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.3.0",
    "@opencode-ai/sdk": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  }
}
